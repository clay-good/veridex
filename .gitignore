# Rust build artifacts
/target/
**/target/
**/Cargo.lock.bak

# Python / maturin / pyo3
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
venv/
.venv/
*.egg-info/
build/
dist/
wheels/
*.whl

# OS
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
*.bak
*.orig

# Claude Code / local-only tooling
.claude/
.codelicious/

# Secrets and signing keys (never commit)
.env
.env.*
*.pem
*.key
keys.json

# Veridex local outputs (do not commit generated artifacts)
# Signed certificates, exported provenance, and reports produced during local runs.
*.veridex.json
*.veridex.cose
# `veridex attest` writes `<dataset>.attestation.json` into the working directory, so running it
# from inside a checkout leaves a signed claim beside the source. It is generated, and signed with a
# key that is itself ignored above — committing one publishes a claim nobody meant to publish.
*.attestation.json
reports/
out/

# Local datasets / fixtures that are large (keep small committed test fixtures)
/data/
!**/tests/fixtures/**

# Downloaded CI log dumps
logs_*/
log.log
